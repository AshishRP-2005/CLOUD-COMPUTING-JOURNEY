#ok so i need to write better and cleaner md files
#can potentially ask chatgpt to do this for me, but then again where is the fun in that
#moving on

#Installation

-Applications are called packages
apt - this command handles installation and ugradation of packages
sudo - Super user DO - Provides temporary ADMIN level Acess

##examples
-[sudo apt update] - list all packages currently installed
-[apt --upgraedable] - list of all packages that can be upgraded
-[sudo apt install htop] - installs htop {basically install and then package name installs that package}
-[sudo apt autoremove] - Removes unwanted packages 
